Todo App

This app uses Backbone.js to structure the Javascript needed for a simple todo list.

Specs written with Jasmine, using sinon.js for stubs and spies.

Rails is the backend, using Jammit to manage assets.

Build runs on travis-ci.org

Instructions:

1) git clone git@github.com:mjansen401/todo.git
2) gem install bundler
2) bundle install
3) rake db:migrate
4) rails server
5) profit
